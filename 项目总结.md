# EKA2L1 WebAssembly 移植 — 项目总结

> 整理时间：2026-06-10
> 汇总自五轮调试记录：[cursor.md](./cursor.md) → [claude.md](./claude.md) → [claude2.md](./claude2.md) → [kilo.md](./kilo.md) → [claude3.md](./claude3.md)

---

## 一、项目目标

把 EKA2L1（Symbian OS / S60 模拟器）通过 Emscripten 移植到浏览器（WebAssembly），实现：

1. 浏览器中可启动模拟器（SDL2 + WebGL2 / OpenGL ES 3.0）
2. 可上传并安装 ROM（EKA1）/ ROM+RPKG（EKA2）
3. 可安装 SIS 软件包，列出已装 app
4. 点击 app 可运行、有画面、可交互
5. 数据持久化（IndexedDB / IDBFS），刷新页面后设备与 app 列表恢复

## 二、移植架构

| 层 | 桌面版 | WASM 版 |
|---|---|---|
| 前端入口 | Qt（多线程：OS 线程 + Graphics 线程） | `src/emu/web/src/main.cpp`（单主线程 + RAF 主循环） |
| 窗口/输入 | Qt / SDL | SDL2（Emscripten 内置端口） |
| 图形 | OpenGL（独立 graphics 线程跑 `driver->run()`） | WebGL2（GL 调用只能在主线程） |
| CPU 模拟 | dynarmic JIT | dyncom 解释器（WASM 无法 JIT arm 代码） |
| 文件系统 | 本机 FS | MEMFS + IDBFS（挂载 `/eka2l1`，IndexedDB 持久化） |
| 主循环 | 各线程自由阻塞 | `emscripten_set_main_loop`（RAF），每帧 2ms CPU 预算、最多 8 个 slice |
| 页面 UI | — | `src/emu/web/shell.html`（上传 ROM/RPKG/SIS、app 列表、暂停/继续） |

关键约束：**Emscripten 主线程禁止阻塞**（`Atomics.wait` 不可用），所有 `join()` / `future::wait()` / `condition_variable::wait()` 在主线程上都会死锁或忙等。这是整个移植过程中最大的错误来源。

## 三、错误链全记录（24+ 个问题）

### 第 1 轮（cursor.md）：安装崩溃与 libc++ locale 灾难

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | `null function` in `io_system::exist` | 没调 `install_rom` 就进系统初始化 | main.cpp 正确走 `install_rom → set_device(0)` |
| 2 | `Uncaught unwind` + SIS 后列表空 | 多余的 `wasm_main_loop()` | 实现 `wasm_install_package/get_app_list/launch_app` |
| 3 | 数据不持久化 | 无 IDBFS | shell.html 挂载 IDBFS + `FS.syncfs` |
| 4 | `Cannot read properties of undefined (FS)` | preRun 时 FS 未就绪 | 挪到 module ready 回调 |
| 5 | ROM 装载返回 -3 无日志 | spdlog 没接 console；EKA2 需要 ROM+RPKG 双文件 | `toggle_console()`；双文件安装逻辑 |
| 6 | `std::bad_cast` in `dynamic_ifile` | Emscripten libc++ `basic_filebuf` 依赖 locale facets | `std::ifstream` → C `FILE*` 重写 |
| 7/8 | `std::locale` 构造崩溃 | WASM 无 locale 数据库，**任何 locale 构造路径都不可用** | kernel `locale_` 保持 nullptr；uchar.cpp 走 `<cwctype>` C 函数 |
| 9 | yaml-cpp 写出崩溃 | `std::stringstream` 构造 → `ios_base::init()` → locale | WASM 下 yaml-cpp emitter 用 `snprintf` 替代 stringstream |

### 第 2 轮（claude.md / CLAUDE.md）：线程、栈、死锁

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 10 | ntimer 线程内存越界 | 初始内存 16MB 太小 | `INITIAL_MEMORY=256MB` |
| 11 | SDL mutex 越界 | worker 默认栈 64KB 溢出污染堆 | `DEFAULT_PTHREAD_STACK_SIZE=2MB`、`PTHREAD_POOL_SIZE=8` |
| 12 | 多线程同地址越界复现 | **主线程 shadow stack 溢出**（默认 64KB），`ASSERTIONS=1` 暴露 stack cookie 被覆写 | `STACK_SIZE=8MB`、`INITIAL_MEMORY=512MB`、`MAXIMUM_MEMORY=2GB`、`PTHREAD_POOL_SIZE=32` |
| 13 | `set_main_loop_timing` 警告 | main loop 注册前调 `SDL_GL_SetSwapInterval` | WASM 下跳过 |
| 14 | 主线程阻塞 1.4s | 重复 `set_device()` → `ntimer::wipeout()` → `timer_thread_->join()` 阻塞主线程 | `init_emulator()` 不再调 set_device，统一由 `wasm_init_with_rom` 调一次 |
| 15 | app 列表永远空 | `applist` 用线程池 `submit_loop().wait()`，主线程 `Atomics.wait` 死锁 | WASM 下注册表同步加载 |
| 16 | 初始化完成后整体卡死 | `cpu_load_save` 空闲时 `idle_event.wait()`，持有 `mut` 死锁 timer 线程 | WASM 强制 `cpu_load_save = false` |
| 17 | 点 app 黑屏 + mprotect 警告 | WASM 无 `mprotect`，commit 返回失败 → 代码段无法加载 | virtualmem.cpp WASM 分支直接成功；`is_memory_wx_exclusive()` 返回 true |
| 18/19 | 刷新后状态丢失 / postRun 过早调用崩溃 | postRun 在 `callMain()` 之前执行，spd_logger 还是 null | 设备恢复挪到 `callMain()` 之后；`wasm_init_with_rom` 入口加 logger null-check |

### 第 3 轮（claude2.md）：持久化三连坑

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 20 | 改代码不生效 | 浏览器强缓存 7MB `eka2l1.js` | serve.py 加 `Cache-Control: no-store` |
| 21 | `IDBFS syncfs InvalidStateError` | Emscripten IDBFS 缓存的 IDBDatabase handle 在长任务后进入 closing 状态 | monkey-patch `IDBFS.getDB` 每次新开连接 |
| 22 | `devices.yml` 永不写盘 | `install_rpkg/install_rom` 调 `add_new_device()` 但从不调 `save_devices()`（上游 bug，桌面版走另一条路径） | 安装后显式 `dvcmngr->save_devices()` |
| 23 | yaml-cpp 解析抛异常直接 Abort | Emscripten 默认 `-fno-exceptions`，所有 throw 直接 abort | 全量 `-fexceptions` + `-s DISABLE_EXCEPTION_CATCHING=0` + `--clean-first` 重编 |

### 第 4 轮（kilo.md）：app 启动卡死（第一阶段）

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 24 | 刷新后 `devices.yml` 解析失败 | 写出的 YAML 含非法转义（`\D`） | `save_devices` 改单引号标量；`load_devices` 加宽松恢复 parser |
| 25 | 点 app 页面卡死 | 单次 `cpu->run()` 跑完整 timeslice 太久，JS 事件循环无法返回 | ticks 限 20000/次；main_loop 改 wall-clock 预算（多 slice） |
| 26 | FS 返回后仍卡死 | dyncom 解释器内部长时间不返回 | dyncom 加 8ms watchdog，每 1024 指令检查一次，超时 yield |
| — | launch 命令不一致 | web 用 `command_create`，桌面用 `command_open` | 改 `command_open` |

### 第 5 轮（claude3.md）：CPU 100% 性能战

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 27 | 误诊：卡在 `c:\resource\errrd` | 那只是正常的资源 fallback 探测链 | （无需修） |
| 28 | SDL 音频吃满主线程 | Emscripten SDL2 用 `ScriptProcessorNode`，`SDL_ResampleAudio` 在主线程同步跑（48431 采样 → 39） | WASM 下禁用 `SDL_INIT_AUDIO` |
| 29 | 误诊"卡死"：实为 spdlog 淹没 DevTools | 每条日志同步跨 wasm→js 边界 + console.log，DOM 渲染队列爆炸 | WASM 默认日志级别 warn |
| 30 | dyncom `instruction_cache` rehash 吃满 CPU（456542 采样） | `unordered_map` 反复 rehash + 无界增长 | `reserve(64K)` + 超 256K entry 清空 |
| 31 | **仍未解决**：第二个 100% CPU 热点 | 待 per-frame 计时数据（已埋点 loop/swap/total） | — |

## 四、本轮新发现（2026-06-10）：渲染与输入链路根本没接

排查"app 启动后黑屏"时发现，**WASM 前端缺了两条核心链路**，这才是黑屏的真正根因（mprotect、launch 命令等都只是前置问题）：

### 1. 渲染链路缺失

EKA2L1 的渲染模型：窗口服务器（winserv）把绘制命令组装成 `command_list`，`submit_command_list()` 推入 `request_queue`；**必须有消费者执行 `dispatch()` 才会发生真正的 GL 调用**。

- 桌面版：独立 graphics 线程跑 `graphics_driver->run()` 阻塞消费队列（[qt/src/thread.cpp:250](src/emu/qt/src/thread.cpp#L250)）
- Android 版：另外还给每个屏幕注册 `add_screen_redraw_callback`，回调里把 `scr->screen_texture` 画到 backbuffer 并 `present()`（[android .../state.cpp:62](src/emu/android/app/src/main/cpp/src/state.cpp#L62)）
- **WASM 版：driver 创建后队列从未被消费，重绘回调也从未注册** → 所有 GL 命令积压，永远黑屏

更严重的连锁：`request_queue::push()` 在队列满 128 条时**阻塞等消费者**（[common/queue.h:177](src/emu/common/include/common/queue.h#L177)）。WASM 上没有消费者，主线程一旦在 push 处阻塞就是永久死锁/忙等——很可能与"第二个 CPU 100% 热点"有关。

### 2. 输入链路缺失

`sdl_web_window` 在 `init_emulator()` 里是局部变量，函数返回即销毁；input hooks（`raw_mouse_event` / `button_pressed` 等）从未挂接；main_loop 里 `SDL_PollEvent` 把除 QUIT 外的所有事件直接丢弃 → **鼠标/触摸/键盘永远到不了 winserv**。

### 修复方案（本轮实施）

1. `request_queue` 增加非阻塞 `try_pop()`；`graphics_driver` 基类增加 `virtual void pump()`，OGL driver 实现为排空队列并 dispatch —— 主线程每帧 RAF 时调用（GL 调用天然回到主线程，符合 WebGL 约束）
2. WASM 下 `max_pending_count_` 调大，杜绝 push 阻塞死锁
3. 仿照 Android `register_draw_callback` 注册屏幕重绘回调：组装"清屏 + 把 screen_texture 等比缩放画到窗口 + present"命令列表提交（回调可能跑在 timer 线程，只组命令不碰 GL，安全）
4. `sdl_web_window` 保活到全局状态，挂接 mouse/key hooks → `winserv->queue_input_from_driver()`；main_loop 改用 `window->poll_events()`

## 五、关键经验（移植 checklist）

1. **主线程不可阻塞**：`join/wait/lock` 全部要审计。生产者-消费者队列若两端都可能在主线程，必须非阻塞化。
2. **`std::locale` / `std::stringstream` / `std::ifstream` 在 Emscripten libc++ 中都可能崩**：locale 数据库不存在；热路径用 C stdio / snprintf。
3. **默认栈太小**：主线程 64KB → 8MB；worker 64KB → 2MB。复杂 C++ 模板调用链轻松超 64KB。
4. **`-fexceptions` 必须全量重编**，只给最终 target 加不够。
5. **WASM 无 mprotect / 无 JIT**：内存保护走 W^X 兼容路径；CPU 只能用解释器（dyncom）。
6. **`-s ASSERTIONS=1` 是最好的诊断工具**：stack cookie 报告比裸的 "memory access out of bounds" 信息量大一个数量级。
7. **性能诊断**：先关 spdlog（WASM 下每条日志 = 同步跨边界 + console DOM 渲染）；用浏览器任务管理器看真实 CPU；Performance trace 样本数 ≠ 调用次数。
8. **热路径 `unordered_map` 要 reserve + 限容**，rehash 在 dlmalloc 多线程加锁下代价巨大。
9. **postRun ≠ main() 已运行**；IDBFS 的 db handle 会过期；浏览器会强缓存大 js 文件。
10. **对照成熟移植端（Android/Qt）逐条核对前端职责清单**：窗口、输入、渲染队列消费、屏幕合成、音频、生命周期回调——漏一条就是"黑屏/没声/没输入"。

## 六、构建与测试

```bash
# 配置（首次）
emcmake cmake -B build_wasm_test -G Ninja -DCMAKE_BUILD_TYPE=Release \
  -DEKA2L1_BUILD_TOOLS=OFF -DEKA2L1_BUILD_TESTS=OFF \
  -DEKA2L1_ENABLE_SCRIPTING_ABILITY=OFF -DCMAKE_POLICY_VERSION_MINIMUM=3.5

# 编译（改了编译选项要 --clean-first）
cmake --build build_wasm_test --target eka2l1_wasm -j4

# 运行（serve.py 带 COOP/COEP 头 + no-store）
cd build_wasm_test/bin
python3 ../../src/emu/web/serve.py 8080 .
```

## 七、当前状态

### 已完成
- [x] WASM 构建链（Emscripten + pthreads + WebGL2 + IDBFS）
- [x] ROM / RPKG / SIS 安装，设备注册与持久化
- [x] 刷新后设备与 app 列表恢复
- [x] app 可启动：进程创建、服务初始化、Avkon/ECom/CDL 全链路推进
- [x] 主线程不再死锁（frame budget + dyncom watchdog）
- [x] 渲染链路（driver pump + 屏幕合成 + present）—— 本轮补齐，待浏览器验证
- [x] 输入链路（mouse/touch/key → winserv）—— 本轮补齐，待浏览器验证

### 待办 / 待验证
- [ ] 浏览器实测：画面是否出来、点击/按键是否生效
- [ ] CPU 100% 第二热点：渲染修复后重新 profile（队列 push 阻塞嫌疑已排除后再看 dyncom）
- [ ] 窗口服务器遗留警告：`Object handle is invalid`、`Unable to find parent for new group with ID = 0x0`、IPC packed struct 72 vs 64
- [ ] 音频：SDL 音频已禁用，后续可考虑 AudioWorklet 方案
- [ ] dyncom 解释器性能优化（复杂 app 帧率）
- [ ] 长时间运行稳定性（内存增长、IDBFS 定期同步）
